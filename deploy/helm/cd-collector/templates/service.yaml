apiVersion: v1
kind: Service
metadata:
  name: {{ include "cd-collector.fullname" . }}
  labels:
    {{- include "cd-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cd-collector.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
